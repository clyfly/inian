# Setup everything for u
echo "Create dir for your build"
mkdir android
cd android

# Start Build rom just relax
echo "start your build sir, wait"
wget -
chmod +x build_rom.sh
./build_rom.sh

# Upload build
wget 